fragment_downloaded_cb({"url":"design/2008_design/2008_UI_Design.html#adding-a-clip","fragment":"Adding a Clip\n The user chooses the clip from the clip browser by\nclicking and dragging.\n When the object enters the timeline, the timeline\nresponds by showing how the timeline will change. In this case the clip\nhas both audio and video streams, so objects appear in both audio and\nvideo tracks.\n The user can move objects to desired layers and\ntime offsets.\n By holding the appropriate modifier key, the user\ncan push existing objects out of the way...\n ...or add a source into a new layer.\n"});